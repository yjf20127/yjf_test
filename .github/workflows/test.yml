name: test

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a
        run: echo "hello"
      - name: multiline
        run: |
          python3 https://github.com/yjf20127/yjf_test/blob/main/main.py
